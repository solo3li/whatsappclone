name: Backend CI/CD

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore Dependencies
        run: dotnet restore server/WhatsappClone.slnx

      - name: Build
        run: dotnet build server/WhatsappClone.slnx --configuration Release --no-restore

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker Image locally
        uses: docker/build-push-action@v4
        with:
          context: ./server
          push: false
          tags: ${{ secrets.DOCKER_USERNAME }}/whatsapp-clone-backend:latest
          load: true

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker Image to Docker Hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/whatsapp-clone-backend:latest

      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: 47.84.69.17
          username: root
          password: ${{ secrets.VPS_PASSWORD }}
          script: |
            docker pull ${{ secrets.DOCKER_USERNAME }}/whatsapp-clone-backend:latest
            docker rm -f whatsapp-backend || true
            docker run -d \
              --name whatsapp-backend \
              -p 8080:8080 \
              -e HOST_MAIL="${{ secrets.HOST_MAIL }}" \
              -e HOST_MAIL_PASS="${{ secrets.HOST_MAIL_PASS }}" \
              -e ConnectionStrings__DefaultConnection="Data Source=/app/data/whatsapp.db" \
              -v /root/whatsapp-data:/app/data \
              ${{ secrets.DOCKER_USERNAME }}/whatsapp-clone-backend:latest
